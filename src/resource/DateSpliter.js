import Calendar from "./calendar";
import EventModal from "./CalendarDetail";
import useEventManager from "./EventManager";
const DateSpliter = ({date}) => {
    const dateArray = {date};
    return(
        <div>
        {date}
        </div>
    )
}
export default DateSpliter;